#!/bin/bash

readme_transforms+=(
	--discard '* windows *'
	--discard '* os x *'
)

has_elf_rpath=1

shared_linker_flags+=( "${unix_linker_flags[@]:+${unix_linker_flags[@]}}" )
exe_linker_flags+=( "${unix_linker_flags[@]:+${unix_linker_flags[@]}}" )
