#!/bin/bash

inherit_projects+=( 'innoextract' )

cmake_options+=(
	-DICON_TYPE=png
	-DUSE_X11=1
)

required_binaries+=(
	arx-install-data
)

copy_files+=(
	
	# Create a data subdirectory (with a README)
	[':data/README.md']='data/'
	
)
